<section class="top-view index">
    <div class="row" style="width: 100%;">
        <div class="description col-md-7">
            <h1>まいぶくは<br>ブックマーク管理共有サービスです。</h1>
            <p>たくさんのお気に入りのWebサイトを登録、シェアしましょう。</p>
        </div>
        <div class="card col-md-5" style="width: 30rem;">
            <div class="card-body">
                <h1 class="card-title">アカウント登録</h1>
                <h2>ブックマーク管理共有アプリ「まいぶく」</h2>
                <%= link_to '採用ご担当者様ログインフォーム', new_user_session_path, id: @user.id, name: @user.name,password: @user.password, class: 'btn btn-warning mb-2' %>

                <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
                    <%= render "devise/shared/error_messages", resource: resource %>
                    <div class="form-group">
                        <%= f.label :name, "アカウント名", for: 'name' %>
                        <%= f.text_field :name, autofocus: true, autocomplete: "off", class: 'form-control', id: 'name', type: 'name' %>
                    </div>
                    <div class="form-group">
                        <%= f.label :email, for: 'email' %>
                        <%= f.email_field :email, autofocus: true, autocomplete: "off", class: 'form-control', id: 'email' %>
                    </div>
                    <div class="form-group">
                        <%= f.label :password, for: 'password' %>
                        <% if @minimum_password_length %>
                            <em>(<%= @minimum_password_length %> 文字以上)</em>
                        <% end %>
                        <%= f.password_field :password, autocomplete: "new-password", class: 'form-control', id: 'password'%>
                    </div>
                    <div class="form-group">
                        <%= f.label :password_confirmation, for:'password_confirmation' %><br />
                        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control', id: 'password_confirmation' %>
                    </div>
                    <div class="form-group">
                        <%= f.button "Sign up", class: 'btn btn-primary' %>
                    </div>
                <% end %>

                <p class="card-text">新規登録済みの方はこちら<i class="far fa-hand-point-down"></i></p>
                <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary' %>
            </div>
        </div>
    </div>
</section>

<%#=======余力あれば作成========%>
<%#<section class="index">
    <h2>まいぶくはブックマークを管理します。</h2>
    <div class="row">
        <div class="col-md-12">
            <p>登録、削除、共有は簡単</p>
            <%= image_tag 'content_view.png',class: 'content_view_image' %>
        <%#</div>
    </div>

</section>%>

